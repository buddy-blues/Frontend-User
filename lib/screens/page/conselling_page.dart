import 'package:flutter/material.dart';

class ConsellingPage extends StatelessWidget {
  const ConsellingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Conselling"),
      ),
    );
  }
}
